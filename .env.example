# SafeTube Environment Variables
# Copy this file to .env and fill in your actual values

# YouTube API Configuration (Fallback for Development/Testing)
# PRIMARY METHOD: Use Main Settings tab in admin interface (Admin → Main Settings)
# This environment variable is only used as fallback for development/testing
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Priority: Main Settings (database) → YOUTUBE_API_KEY → Error
YOUTUBE_API_KEY=your_youtube_api_key_here

ELECTRON_LOG_VERBOSE=false

# Dev Tools Control
# Set to 'true' to automatically open Chrome DevTools in production builds
# Useful for debugging. Example: OPEN_DEV_TOOLS=true
# OPEN_DEV_TOOLS=false

# Development Configuration
# NODE_ENV=development

# CI/CD Configuration
# DO NOT set CI=true here for normal development!
# Only set CI=true in your shell or .env if you want to skip integration tests locally.
# CI=true is set automatically in CI environments (GitHub Actions, Docker, etc.)
#CI=true

# Optional: Custom YouTube cookies for yt-dlp
# YT_DLP_COOKIES_FILE=path/to/cookies.txt

# Optional: Debug logging
# DEBUG=true
